{
  "builds": [
    {
      "src": "app.py",
      "use": "python"
    }
  ],
  "routes": [
    {"src": "/api/(.*)", "dest": "/api/$1"},
    {"src": "/(.*)", "dest": "/app.py"}
  ],
  "env": {
    "MY_ENV_VAR": "value"
  }
}